出品した商品
%li
出品中、取引中、売却済み
%li
- @products.each do |p|
  %p.products
    -# = image_tag p.image.url
    = image_tag p.image
    = link_to p.title, product_path(p.id)
  line ------------------------------